// common screed styles

$screed-font-size ?= 12pt
$screed-line-height ?= normal
$screed-bg-color ?= rgba(250, 250, 250, 0) // transparent
$screed-info-bg-color ?= rgba(250, 250, 250, .5) // half transparent white
$screed-in-focus-box-shadow ?= 0 0 20px -4px rgba(0, 200, 0, .5) inset, 0 0 20px -4px rgba(0, 200, 0, .5)
$screed-selection-color ?= rgb(200, 0, 0) // red
$screed-selection-bg-color ?= rgba(200, 0, 0, .05)
$screed-doc-bg-color ?= rgba(225, 225, 225, 1)
$screed-border-color ?= rgba(35, 35, 35, .1)
$screed-panel-bg-color ?= rgba(200, 200, 200, .3)

@import './d-screed'

@import "./styles/on-paper"
@import "./styles/insider"
